import 'package:flutter/material.dart';

class Widget034 extends StatelessWidget {
  const Widget034({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Chip(
          label: Text('This is a chip in flutter'),
        ),
      ),
    );
  }
}
